## Change

Describe the user-visible problem and resulting behavior.

## Verification

List the checks run and any checks that still require hardware or credentials.
Use isolated files/profiles for file-operation and installer tests.

For an overhaul release, link the completed gates in `docs/OVERHAUL.md`.
Do not mark a preview release-ready based only on a successful build.
