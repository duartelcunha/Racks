# Racks signed update feeds

Feeds and package signatures use the public key recorded in installer/update-public-key.txt in the source repository. Production private keys never belong in this branch. Beta and stable feeds are separate.
