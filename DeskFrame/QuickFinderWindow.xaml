<ui:FluentWindow x:Class="DeskFrame.QuickFinderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        ExtendsContentIntoTitleBar="True"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        Background="#E61E1E1E"
        Foreground="White"
        ResizeMode="NoResize"
        Title="Find in racks"
        Width="640"
        Height="440"
        Deactivated="Window_Deactivated"
        KeyDown="Window_KeyDown">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Find in racks"
                   Foreground="#AAA" FontSize="11" Margin="0,0,0,4"/>

        <TextBox x:Name="QueryBox"
                 Grid.Row="1"
                 FontSize="16"
                 Padding="6"
                 Background="#222"
                 Foreground="White"
                 BorderBrush="#444"
                 BorderThickness="0,0,0,1"
                 TextChanged="QueryBox_TextChanged"
                 PreviewKeyDown="QueryBox_PreviewKeyDown"/>

        <ListBox x:Name="Results"
                 Grid.Row="2"
                 Margin="0,8,0,0"
                 Background="Transparent"
                 BorderThickness="0"
                 Foreground="White"
                 MouseDoubleClick="Results_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                        <Image Width="20" Height="20" Source="{Binding Item.Thumbnail}" Margin="0,0,8,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding Item.DisplayName}" FontSize="13"/>
                            <TextBlock Text="{Binding Subtitle}" FontSize="10" Foreground="#888"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="3"
                   Text="↑↓ navigate    Enter open    Esc dismiss"
                   Foreground="#666"
                   FontSize="10"
                   Margin="0,8,0,0"/>
    </Grid>
</ui:FluentWindow>
